[pytest]
# --assert=plain and -p no:cacheprovider are required when running from a
# Databricks workspace (FUSE mount does not support __pycache__ creation).
# These flags are harmless in GitHub Actions where __pycache__ is allowed.
addopts = -p no:cacheprovider --assert=plain
testpaths = src/lib/tests
